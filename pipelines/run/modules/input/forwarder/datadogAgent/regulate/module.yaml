#
#  🔟❎ Datadog Agent Regulate module

module:

  name: datadogAgentRegulate
  title: Datadog Agent reducer
  description: regulate events before shipping to Datadog via Datadog Agent

  # 'doc' specifies the markdown doc content, only required when generating docs.
  doc: '+include $=TenXEnv.get("docGenerate") ? "run/modules/input/forwarder/datadogAgent/regulate/doc.md":""'
