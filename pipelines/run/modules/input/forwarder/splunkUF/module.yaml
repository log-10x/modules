#
# 🔟❎ 'run' Splunk Universal Forwarder input module

module:

  name: splunkUF
  title: Splunk UF input
  description: regulate and optimize events before shipping to Splunk via Universal Forwarder

  moduleSort: [splunkUFRegulate]

  # 'doc' specifies the markdown doc content, only required when generating docs.
  doc: +include $=TenXEnv.get("docGenerate") ? "run/modules/input/forwarder/splunkUF/doc.md":""
