#
# 🔟❎ 'run' Datadog Agent input module

module:

  name: datadogAgent
  title: Datadog Agent input
  description: regulate and optimize events before shipping to Datadog via Datadog Agent

  moduleSort: [datadogAgentRegulate]

  # 'doc' specifies the markdown doc content, only required when generating docs.
  doc: +include $=TenXEnv.get("docGenerate") ? "run/modules/input/forwarder/datadogAgent/doc.md":""
