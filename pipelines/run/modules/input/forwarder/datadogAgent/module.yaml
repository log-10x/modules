#
# 🔟❎ 'run' Datadog Agent input module

module:

  name: datadogAgent
  title: Datadog Agent input
  description: receive and optimize events before shipping to Datadog via Datadog Agent

  # 'configFolder' specifies a folder containing default configuration files for the settings defined by this unit/module
  configFolder: run/input/forwarder/datadogAgent

  # 'doc' specifies the markdown doc content, only required when generating docs.
  doc: +include $=TenXEnv.get("docGenerate") ? "run/modules/input/forwarder/datadogAgent/doc.md":""
