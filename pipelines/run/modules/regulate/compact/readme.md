## 1️⃣0️⃣❎ Compact regulator module

Gate per-event `encode()` vs `fullText` emission via a declarative field-set keyed lookup file. Entries are GitOps-controlled (MCP writes them via PR) and hot-reloaded without engine restart.

To learn more see the Compact regulator [module documentation](https://doc.log10x.com/run/regulate/compact "Gate per-event encode() vs fullText via a declarative field-set keyed lookup file.").
