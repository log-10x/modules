## 1️⃣0️⃣❎ Input extractor unit

Filter, redact, and capture events from an input stream to transform into TenXObjects.

To learn more see the Input extractor [unit documentation](https://doc.log10x.com/run/input/extract "Filter, redact, and capture events from an input stream to transform into TenXObjects").

