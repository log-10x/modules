# 🔟❎ 'run' Fluent Bit forward output stream configuration

# This configuration defines a Unix domain socket output to write
# encoded/plain-text events to a Fluent Bit forwarder process via the native Fluentd 'forward' protocol.

# To learn more see: https://github.com/fluent/fluent-bit-docs/blob/master/pipeline/outputs/forward.md

# Set the 10x pipeline to 'run'
tenx: run

# =============================== Dependencies ================================

include:

  - source: match
    options:

      # Capture rawOutputSocketPort from tenx-unix.conf as an 10x config variable
      path: run/modules/input/forwarder/fluentbit/conf/tenx-unix.conf
      regex: "^\\s*@SET\\s+forwardAddress\\s*=\\s*(?<rawOutputForwardAddress>.*?)$"

    # load the forward protocol over the Unix domain socket module
  - run/modules/output/event/forward

# ============================= Output Options ================================

# 'outputForward' defines the output(s) used to write back to the Fluent Bit forwarder
outputForward:

    # Regulate-only mode (fluentbitEncodeObjects=false): every event goes out as fullText.
  - writeObjects: /$=!TenXEnv.get("fluentbitEncodeObjects")

    address: $=rawOutputForwardAddress

    fields:
      - fullText
      - $?fluentbitOutputFields
    encodeType: $?fluentbitOutputEncodeType
    encodeDelimiter: ","

    # Do not encode templates
    writeTemplates: false

    # Compact-capable, compact module NOT active: legacy all-encode behavior.
    # Every event emits via encode(). Matches pre-compact-module semantics so
    # `fluentbitEncodeObjects=true` alone (without loading the compact module)
    # is unchanged.
  - writeObjects: /$=TenXEnv.get("fluentbitEncodeObjects") && !TenXEnv.get("compactRegulatorLookupFile")

    address: $=rawOutputForwardAddress

    fields:
      - encoded=encode()
      - $?fluentbitOutputFields
    encodeType: $?fluentbitOutputEncodeType
    encodeDelimiter: ","

    writeTemplates: /$=TenXEnv.get("fluentbitEncodeObjects")

    # Compact-capable, compact module active: per-event decision via
    # `shouldEncode()`. Gated on `compactRegulatorLookupFile` being set so the
    # engine only static-analyzes this fields expression when the CompactObject
    # class is loaded (shouldLoad gates on the same condition).
  - writeObjects: /$=TenXEnv.get("fluentbitEncodeObjects") && TenXEnv.get("compactRegulatorLookupFile")

    address: $=rawOutputForwardAddress

    fields:
      - 'output=shouldEncode() ? encode() : fullText'
      - $?fluentbitOutputFields
    encodeType: $?fluentbitOutputEncodeType
    encodeDelimiter: ","

    writeTemplates: /$=TenXEnv.get("fluentbitEncodeObjects")
