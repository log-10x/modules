## 1️⃣0️⃣❎ Logstash JSON socket input module

Read events from Logstash over newline-delimited JSON (TCP or Unix domain socket).

To learn more see the Logstash JSON socket input [module documentation](https://doc.log10x.com/run/input/forwarder/logstash/input "Read events from Logstash over newline-delimited JSON (TCP or Unix domain socket)").

