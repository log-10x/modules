#
#  🔟❎ Splunk UF Optimize module

module:

  name: splunkUFOptimize
  title: Splunk UF optimizer
  description: optimize events before shipping to Splunk via Universal Forwarder

  # 'doc' specifies the markdown doc content, only required when generating docs.
  doc: '+include $=TenXEnv.get("docGenerate") ? "run/modules/input/forwarder/splunkUF/optimize/doc.md":""'
