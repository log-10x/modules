## 1️⃣0️⃣❎ Forward protocol input module

Read events via the Fluent Forward protocol on a Unix domain socket or TCP port.

To learn more see the Forward protocol input [module documentation](https://doc.log10x.com/run/input/forward "Read events via the Fluent Forward protocol on a Unix domain socket or TCP port").

