#
# 🔟❎ 'run' Filebeat input module

module:

  name: filebeat

  title: Filebeat input

  description: report, regulate, and optimize events collected by Filebeat forwarders

  moduleSort: [filebeatRegulate]

  # 'doc' specifies the markdown doc content, only required when generating docs.
  doc: +include $=TenXEnv.get("docGenerate") ? "run/modules/input/forwarder/filebeat/doc.md":""
