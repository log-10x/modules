#
#  🔟❎ Splunk UF Report module

module:

  name: splunkUFReport
  title: Splunk UF reporter
  description: report on events before shipping to Splunk via Universal Forwarder

  # 'doc' specifies the markdown doc content, only required when generating docs.
  doc: '+include $=TenXEnv.get("docGenerate") ? "run/modules/input/forwarder/splunkUF/report/doc.md":""'
