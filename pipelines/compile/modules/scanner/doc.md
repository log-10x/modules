Parses source code and binaries to extract [symbol](https://doc.log10x.com/run/transform/structure/#symbols) tokens with their context (class name, function name, code constant).

Scanners generate and traverse ASTs to locate symbols and capture surrounding context metadata.
