## 1️⃣0️⃣❎ Rate receiver module

Filter TenXObjects from output using either local per-node budget sampling or a declarative field-set keyed mute file.

To learn more see the rate receiver [module documentation](https://doc.log10x.com/run/receive/rate "Filter TenXObjects from output using either local per-node budget sampling or a declarative field-set keyed mute file.").

