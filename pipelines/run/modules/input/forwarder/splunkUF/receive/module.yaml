#
#  🔟❎ Splunk UF Receive module

module:

  name: splunkUFReceive
  title: Splunk UF Receiver
  description: receive events from the Splunk Universal Forwarder for filtering and compaction

  # 'doc' specifies the markdown doc content, only required when generating docs.
  doc: '+include $=TenXEnv.get("docGenerate") ? "run/modules/input/forwarder/splunkUF/receive/doc.md":""'
