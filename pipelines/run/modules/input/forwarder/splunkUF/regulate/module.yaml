#
#  🔟❎ Splunk UF Regulate module

module:

  name: splunkUFRegulate
  title: Splunk UF reducer
  description: regulate events before shipping to Splunk via Universal Forwarder

  # 'doc' specifies the markdown doc content, only required when generating docs.
  doc: '+include $=TenXEnv.get("docGenerate") ? "run/modules/input/forwarder/splunkUF/regulate/doc.md":""'
