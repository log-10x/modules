## 1️⃣0️⃣❎ Object storage index output module

Index files uploaded to a object storage container (e.g. AWS S3 bucket).

To learn more see the Object storage index output [module documentation](https://doc.log10x.com/run/input/objectStorage/index "Index files uploaded to a object storage container (e.g. AWS S3 bucket).").

