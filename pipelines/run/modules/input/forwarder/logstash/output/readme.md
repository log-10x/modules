## 1️⃣0️⃣❎ Logstash JSON socket output module

Send processed events back to Logstash over a newline-delimited JSON TCP socket.

To learn more see the Logstash JSON socket output [module documentation](https://doc.log10x.com/run/input/forwarder/logstash/output "Send processed events back to Logstash over a newline-delimited JSON TCP socket").

