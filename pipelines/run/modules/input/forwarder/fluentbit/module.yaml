#
# 🔟❎ 'run' Fluent Bit input module

module:

  name: fluentbit

  title: Fluent Bit input

  description: regulate and optimize events collected by Fluent Bit forwarders

  moduleSort: [fluentbitRegulate]

  # 'doc' specifies the markdown doc content, only required when generating docs.
  doc: +include $=TenXEnv.get("docGenerate") ? "run/modules/input/forwarder/fluentbit/doc.md":""
