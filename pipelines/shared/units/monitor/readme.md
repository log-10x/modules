## 1️⃣0️⃣❎ Pipeline monitor unit

Emit pipeline debug values to log and the 10x time series backend.

To learn more see the Pipeline monitor [unit documentation](https://doc.log10x.com/run/monitor "Emit pipeline debug values to log and the 10x time series backend").

