#
# 🔟❎ 'compile' Scalameta source code scanner module

module:

  name: scalameta

  title: Scalameta scanner

  description: scan Java source code for symbol values using the Scalameta library
  
  # 'doc' specifies the markdown doc content, only required when generating docs.
  doc: +include $=TenXEnv.get("docGenerate") ? "compile/modules/scanner/scalameta/doc.md":""
