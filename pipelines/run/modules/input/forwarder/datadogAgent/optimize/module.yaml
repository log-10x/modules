#
#  🔟❎ Datadog Agent Optimize module

module:

  name: datadogAgentOptimize
  title: Datadog Agent optimizer
  description: optimize events before shipping to Datadog via Datadog Agent

  # 'doc' specifies the markdown doc content, only required when generating docs.
  doc: '+include $=TenXEnv.get("docGenerate") ? "run/modules/input/forwarder/datadogAgent/optimize/doc.md":""'
