### Install

=== "Linux/macOS"

    See the Log10x Receiver Vector [run instructions](https://doc.log10x.com/apps/receiver/run/#vector)

=== "k8s"

    Deploy to k8s via [Helm](https://helm.sh/) — Vector chart fork with the `tenx.*` block.
