#
#  🔟❎ Datadog Agent Report module

module:

  name: datadogAgentReport
  title: Datadog Agent reporter
  description: report on events before shipping to Datadog via Datadog Agent

  # 'doc' specifies the markdown doc content, only required when generating docs.
  doc: '+include $=TenXEnv.get("docGenerate") ? "run/modules/input/forwarder/datadogAgent/report/doc.md":""'
