#
# 🔟❎ 'run' OpenTelemetry Collector input module

module:

  name: otelCollector

  title: OpenTelemetry Collector input

  description: regulate and optimize events collected by OpenTelemetry Collector forwarders

  moduleSort: [otelCollectorRegulate]

  # 'doc' specifies the markdown doc content, only required when generating docs.
  doc: +include $=TenXEnv.get("docGenerate") ? "run/modules/input/forwarder/otel-collector/doc.md":""

